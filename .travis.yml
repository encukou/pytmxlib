# See http://travis-ci.org/encukou/pytmxlib

language: python
python:
  - "2.6"
  - "2.7"
  - "3.1"
  - "3.2"
install:
  - python setup.py develop
  - '[ "$WITH_LXML" = "yes" ] && sudo apt-get install python-lxml; :'
  - pip install pytest-cov
env:
  - USE_LXML=no
  - USE_LXML=yes
script: py.test
